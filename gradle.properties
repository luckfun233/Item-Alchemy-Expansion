# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# IntelliJ IDEA is not yet fully compatible with configuration cache, see: https://github.com/FabricMC/fabric-loom/issues/1349
org.gradle.configuration-cache=false

# Fabric Properties
# check these on https://fabricmc.net/develop
minecraft_version=1.21.1
yarn_mappings=1.21.1+build.3
loader_version=0.18.6
loom_version=1.14-SNAPSHOT

# Mod Properties
mod_version=1.1.1
maven_group=itemalchemy.expansion
archives_base_name=itemalchemy-expansion

# Dependencies
fabric_version=0.107.0+1.21.1

# --- Addon target (Item Alchemy) + its dependency (mcpitanlib) ---
# 本地构建（见 research/item-alchemy-main 与 research/MCPitanLib-1.21.1）：
#   itemalchemy 1.3.3-SNAPSHOT 来自 research/item-alchemy-main 的 publishToMavenLocal
#   mcpitanlib 3.7.2 来自 research/MCPitanLib-1.21.1 的 publishToMavenLocal
# 注：Maven 标识符仅允许 [A-Za-z0-9_\-.]，故 artifactId 用 '.' 代替 '+'。
itemalchemy_version=1.3.3-SNAPSHOT
mcpitanlib_version=3.7.2

# --- Optional config UI (soft deps) ---
cloth_config_version=15.0.140
modmenu_version=11.0.2
