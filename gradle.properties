# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

# IntelliJ IDEA is not yet fully compatible with configuration cache, see: https://github.com/FabricMC/fabric-loom/issues/1349
org.gradle.configuration-cache=false

# Fabric Properties
# check these on https://fabricmc.net/develop
minecraft_version=1.20.1
yarn_mappings=1.20.1+build.10
loader_version=0.19.3
loom_version=1.14.10

# Mod Properties
mod_version=1.1.1
maven_group=itemalchemy.expansion
archives_base_name=itemalchemy-expansion

# Dependencies
fabric_version=0.92.11+1.20.1

# --- Addon target (Item Alchemy) + its dependency (mcpitanlib) ---
# Verify on https://maven.pitan76.net/v/#net/pitan76/itemalchemy/ and .../mcpitanlib-fabric+1.20.1/
# NOTE: mcpitanlib 3.6.5 has a 0-byte (broken) POM on the maven -> use 3.3.2 (valid POM, same 1.20.1 line).
itemalchemy_version=1.1.3
mcpitanlib_version=3.3.2

# --- Optional config UI ---
cloth_config_version=11.1.136
modmenu_version=7.2.2